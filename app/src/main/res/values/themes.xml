<resources>
    <!-- Base application theme. Material Components (not Material3): Material3 needs a newer library than API 17 allows. -->
    <style name="Base.Theme.Inventory" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.Inventory" parent="Base.Theme.Inventory" />
    <style name="InventoryColumnHeader">
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:maxLines">1</item>
    </style>
</resources>
